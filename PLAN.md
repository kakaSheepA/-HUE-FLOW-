# PLAN

## 当前目标

为 `色彩流动(HueFlow)` 建立可独立使用的外部记忆文件，并与 `超级调色板` 保持同构的工作方式，方便后续在不依赖聊天上下文的前提下继续开发。

## 当前进度

- 已建立 `AGENT.md`
- 已建立 `PLAN.md`
- 已把“启动先读 `PLAN.md`、结束必更 `PLAN.md`”写入工作规则
- 已将 `超级调色板` 作为独立 tab 接入宿主插件
- 已补共享消息层与超级调色板运行时
- 已将插件窗口尺寸调整为 `450x600`
- 已将外层视觉切换为更接近超级调色板的主题风格
- 已新增独立主题样式文件 `ui/styles/super-theme.css`
- 已将 `ui/App.vue` 中的大块业务内容拆到独立组件与组合函数中，`App.vue` 只保留页面编排壳
- 已新增 `ui/components/ColorAdjustTab.vue`、`ui/components/PresetGradientsTab.vue`、`ui/composables/useColorAdjust.ts`、`ui/composables/usePresetGradients.ts`、`ui/constants/preset-gradients.ts`
- 已将 `弥散渐变` tab 重做为 mesh gradient 编辑器，主体拆成 `ui/components/DiffuseGradientTab.vue`、`ui/composables/useMeshGradient.ts`、`ui/constants/mesh-gradient-presets.ts`、`ui/utils/color.ts`
- 已让弥散渐变支持预览、色板切换、预设切换、随机化、增删色块与 PNG 保存
- 已按参考图收紧弥散渐变的布局，并让色板点击只承担选中作用
- 已把弥散渐变 tab 的色块与按钮视觉收口到插件整体风格，去掉了过重的独立卡片感
- 已为弥散渐变色板增加“新增颜色”加号按钮，并让新增色块同步生成对应预览点位
- 已为弥散渐变补上删除色块能力，并将所有相关文案统一为中文
- 已将弥散渐变的颜色调整改为浏览器原生颜色选择器，点击色块直接唤起系统控件
- 已把弥散渐变颜色面板相关自绘组件与样式移除，不再保留多余 UI 层级
- 已为超级调色板 tab 增加深度样式覆盖，适配当前插件窗口尺寸
- 已将默认首屏切换为超级调色板，窗口布局以它为主
- 已完成一次构建验证，`npm run build` 通过
- 已在打包层强制统一 `vue` 解析入口，避免从另一个插件目录引入第二份运行时
- 已验证 `超级调色板` 不再白屏，核心面板可正常渲染
- 已修正超级调色板右侧下拉浮层裁切问题，恢复浮层可见
- 已将预设渐变页改为自适应剩余高度，去掉固定 360px 上限
- 已将 `.tab-content` 圆角改为 0
- 已移除 `color-stop-card` 的视觉样式块，仅保留必要布局骨架
- 已将预设色块列数调整为 8 列
- 已将 `.color-dot` 圆角改为 4px
- 已将 `.preset-item` 圆角改为 4px，并去掉阴影
- 已为 `gradient-editor` 增加参照缓动区的网格背景
- 已将 `gradient-editor` 底图改为指定浅色渐变
- 已将 `.preset-item` 圆角去掉
- 已将 `超级调色板` tab 调整到第 2 位
- 已清理重复的 `container.is-super` 超态样式
- 已为右侧两个下拉增加 label 文案
- 已将右侧下拉按钮文字合并到 label，去掉重复显示
- 已把右侧下拉文字放回按钮框内
- 已将按钮文案改成“色阶计算：默认”这种完整格式
- 已将右侧下拉按钮文字字号调小
- 已将右侧下拉按钮文字改为 10px 且不换行省略
- 已为右侧悬浮卡片增加 200ms hover 延迟
- 已将右侧悬浮卡片 hover 延迟改为 500ms
- 已将右侧悬浮卡片 hover 延迟改为 350ms
- 已新增“梯度色阶”tab，支持左侧切换色系、右侧显示梯度色阶列表
- 已新增点击梯度色块后复制并替换选区颜色的消息通道
- 已完成一次构建验证，`npm run build` 通过
- 已移除“梯度色阶”tab 顶部说明区，保留核心色系与色阶列表
- 已移除“梯度色阶”tab 底部提示文案，并让左右两列撑满高度
- 已去掉右侧梯度列表 hover 导致的横向滚动条
- 已将“梯度色阶”tab 的颜色与色值改为按参考图静态写死
- 已将宿主入口收口为单个“梯度色阶”页面，移除其他功能页入口
- 已回退为单个“梯度色阶”tab 壳，保留梯度内容页
- 已恢复多 tab 宿主壳，重新显示色彩调节、超级调色板、梯度色阶、弥散渐变与预设渐变入口
- 已收紧默认壳样式，避免覆盖 `SUPER` 状态下的主题样式
- 已将 `super-theme.css` 的视觉规则限定到 `SUPER` 状态，避免影响 `色彩调节` tab
- 已恢复 `色彩调节` tab 的基础控件样式：提示、分组、滑条、输入框和重置按钮
- 已将弥散渐变编辑区改为实时预览当前色站合成背景，并增加边框
- 已让 `spread` 影响弥散渐变编辑区的实时预览范围
- 已将弥散渐变编辑区的网格改为十字轴样式
- 已为弥散渐变锚点增加 hover 十字线指引
- 已加重弥散渐变编辑区的颜色预览层，避免画面发白
- 已将弥散渐变编辑器改成更接近 mesh gradients 的综合色团预览
- 已将弥散渐变编辑区背景改为十字轴
- 已为弥散渐变锚点补上 hover 十字线指引
- 已将 `SUPER` tab 的选中态改回与其他 tab 一致的底部高亮样式
- 已将 `SUPER` tab 的外壳间距、字号、颜色和 hover 统一为默认 tab 样式
- 已清理超级调色板与宿主侧这轮排查加入的调试日志，保留稳定版输出
- 已将梯度色阶 tab 左侧部分家族名改为更贴近参考图的命名
- 已将梯度色阶中的 `50-DiDi Brand` 改回纯数字 `50`
- 已按提供的 HTML 将梯度色阶首个家族录为 `橙 / Warm Orange`
- 已为梯度色阶左侧家族列表增加副标题展示能力
- 已确认左侧家族名不再添加英文副标题，后续只保留中文
- 已按提供的 HTML 将第二个家族名改为 `黄`
- 已将第二个家族名按更新的 HTML 回退为 `橘黄`
- 已按提供的 HTML 将第三个家族名改为 `绿`
- 已按提供的 HTML 将第四个家族名改为 `青`
- 已按提供的 HTML 将第五个家族名改为 `浅蓝`
- 已按提供的 HTML 将第六个家族名改为 `蓝`
- 已按提供的 HTML 将第七个家族名改为 `蓝紫`
- 已按提供的 HTML 将第八个家族名改为 `紫`
- 已按提供的 HTML 将第九个家族名改为 `粉`
- 已按提供的 HTML 将第十个家族名改为 `红`
- 已按提供的 HTML 将灰色系改成特殊展示格式 `#hex/#000 xx%`
- 已为灰色系补齐 0/2/4/6/12/24/36/60/72/84/100 的完整色阶
- 已将梯度色阶点击逻辑改为先应用颜色、再异步复制，避免复制阻塞上色
- 已为梯度色阶点击与主线程上色链路增加调试日志
- 已修复主线程中缺失的 `hexToRgb`，避免色块点击时报错中断上色
- 已写入规则：同类问题第二次未定位清楚时，优先增加日志排查
- 已为梯度色阶右侧色块增加 hover 抬起效果
- 已为梯度色阶右侧色块增加“已复制”文案并在 2 秒后恢复
- 已将梯度色阶左侧家族栏加宽 20px，并把右侧列表相应收窄
- 已将梯度色阶左侧家族项调整为更普通的 tab 视觉
- 已将梯度色阶左侧家族栏改为不滚动并纵向平分高度
- 已清理梯度色阶与主线程中的残余生产日志，收敛到稳定版
- 已将超级调色板右侧色块点击补齐为“复制 + 应用到选区”的交互
- 已为超级调色板右侧色块点击链路补充日志，并让“已复制”态先于复制完成显示
- 已修复超级调色板 UI 中缺失的 `sendMsgToPlugin` 导入，避免点击时直接抛错
- 已为超级调色板右侧色块应用增加主线程回执，区分消息发送成功和实际写入命中
- 已把宿主 UI->主线程消息包装为 `pluginMessage`，并兼容回执式确认链路
- 已为超级调色板 UI 侧补充主线程消息接收日志，方便确认回执是否真正到达
- 已修复主线程对超级调色板色块消息的匹配条件，兼容 `ui:apply-swatch-color` 与旧枚举值
- 已为梯度色阶中的中性灰补上透明度应用，点击时改为黑色 + alpha 写入形状
- 已修正 MasterGo 纯色透明度字段，改用 `color.a` 而不是 `SolidPaint.alpha`

## 已完成

- 确认当前插件的主要入口与消息层：
  - `manifest.json`
  - `lib/main.ts`
  - `ui/App.vue`
  - `ui/ui.ts`
- 明确本插件当前仍以色彩调节、弥散渐变、预设渐变为主
- 新增 `超级调色板` tab，并接入其独立 UI 与主线程消息流
- 共享消息类型已统一到当前宿主工程
- 超级调色板运行时已从 `main.ts` 中拆出为独立模块
- 宿主样式已从原始蓝色表单风格转为更接近超级调色板的卡片化主题
- 超级调色板 tab 已从整页布局改为嵌入式布局适配
- 默认打开时优先展示超级调色板

## 当前关注项

1. 保持现有三块功能稳定
2. 继续把新增模块边界保持清晰，避免两个 tab 互相侵入
3. 每次新的开发回合都先读本文件，再开始动手
4. 验证当前仓库是否因为双份 Vue 运行时导致 `refs` 崩溃
5. 保持当前合并结构稳定，继续按模块拆分新增内容

## 下一步

- 等待后续具体功能任务
- 若继续打磨双 tab 体验，优先处理布局适配和交互细节，而不是再扩大单文件职责

## 验证记录

- 本轮完成 `npm run build`
- 构建通过，当前宿主能同时打包色彩流动与超级调色板两套能力
- 复测截图确认 `超级调色板` 面板正常显示
- 调试日志已清理，保留稳定版挂载逻辑
- 已修正右侧两个下拉菜单被裁切的问题
- 预设渐变页的 grid 高度已切换为弹性填充
- 卡片视觉样式已清理，功能布局保持不变
- 预设色块已按 8 列显示
- 色点从圆形调整为 4px 圆角
- 预设卡片已去掉阴影，圆角调整为 4px
- 预设编辑器已增加网格背景
- 预设编辑器底图已切换为指定浅色渐变
- 预设卡片已改为直角
- tab 顺序已更新，`超级调色板` 位于第二位
- 超态样式里不再保留重复 padding 规则
- 右侧下拉现在显示“色阶计算：...”和“颜色格式：...”
- 下拉按钮只保留箭头，不再重复展示当前值
- 右侧下拉现在是“文字 + 箭头”同框显示
- 下拉按钮现在直接显示“色阶计算：默认 / 颜色格式：Hex”
- 右侧按钮文字已收小一号
- 右侧按钮文字已固定为 10px 并启用单行省略
- hover 浮层现在延迟 200ms 后才显示
- hover 浮层现在延迟 500ms 后才显示
- hover 浮层现在延迟 350ms 后才显示
- 弥散渐变编辑区已改为实时综合色团预览，并使用十字轴背景
- 弥散渐变锚点 hover 时会出现十字线指引
- 弥散渐变编辑器已进一步向 mesh gradients 风格靠拢
- `SUPER` tab 的激活态已与其他 tab 保持一致的选中样式
- `SUPER` tab 的外层 tab 样式已与其他 tab 保持一致
- 梯度色阶 tab 左侧家族名称已进一步按参考图风格收口
- 梯度色阶的 `50-DiDi Brand` 标签已移除品牌后缀
- 梯度色阶首个家族已切换为 `橙 / Warm Orange` 并保留对应色值
- 梯度色阶左侧家族名已收回为纯中文展示
- 梯度色阶第二个家族已按 HTML 更新为 `黄`
- 梯度色阶第二个家族名已与更新的 HTML 对齐为 `橘黄`
- 梯度色阶第三个家族已按 HTML 更新为 `绿`
- 梯度色阶第四个家族已按 HTML 更新为 `青`
- 梯度色阶第五个家族已按 HTML 更新为 `浅蓝`
- 梯度色阶第六个家族已按 HTML 更新为 `蓝`
- 梯度色阶第七个家族已按 HTML 更新为 `蓝紫`
- 梯度色阶第八个家族已按 HTML 更新为 `紫`
- 梯度色阶第九个家族已按 HTML 更新为 `粉`
- 梯度色阶第十个家族已按 HTML 更新为 `红`
- 灰色系已按 HTML 改为特殊文本格式并补齐完整色阶
- 梯度色阶点击色块时会先立即替换选区颜色
- 梯度色阶色块点击链路现已打点日志，便于排查未生效问题
- 梯度色块点击导致的 `hexToRgb` 未定义错误已修复
- 后续遇到重复未定位问题时，先补日志再继续排查
- 梯度色块 hover 时会有抬起反馈，点击后会短暂显示“已复制”
- 梯度色阶左侧栏现在更宽，右侧列表更窄，视觉更接近普通 tab
- 梯度色阶左侧栏已改为固定高度分配，不再显示滚动条
- 当前版本保留最少量必要日志，避免干扰正常交互
- 当前版本已去除这轮排查中的调试日志，控制台只保留必要输出
- 超级调色板右侧色块现在会同步把 swatch 颜色写入当前选区
- 超级调色板右侧色块点击现在更容易通过日志定位消息发送与主线程接收情况
- 超级调色板右侧色块点击链路的异常现在已定位到具体导入缺失并修复
- 超级调色板右侧色块是否真正改到图层现在可以从回执里的命中数判断
- 宿主端点击回执现在应该能区分“消息到达”和“属性写入命中数”
- 现在可以先看 UI 的 `bridge message received`，再看 `bridge swatch applied`，最后才判断写入结果
- 之前色块应用无效的根因是主线程还在匹配旧枚举名，导致分支没有执行
- 中性灰现在不会再把展示用灰阶值直接写入形状，而是按透明度应用
- 中性灰的透明度现在应按 0/2/4/.../100% 正确落到图层上
- 中性灰右侧文案已拆成固定宽度的左值和透明度尾巴，展示更规整
- 梯度色阶右侧色块点击现在只应用填充，不再写入描边
- 梯度色阶左侧新增了“渐变”切换，右侧先接了默认渐变列表
- 梯度色阶里新增的渐变列表现在按 106deg 生成线性渐变 transform
- 梯度色阶左侧家族顺序已调整为中性灰、橙、蓝优先
- 已清理本轮确认无用的实色/渐变工厂函数，并修复超级调色板外部引用路径
- 弥散渐变 tab 现在是简化版 mesh gradient 编辑器，颜色面板已经删除
- `ui/App.vue` 的样式已全部抽离到单独的 `ui/styles/app.css`
- `ui/App.vue` 的功能逻辑也要按模块拆分到独立文件，后续新功能不再继续堆进 `App.vue`
- `ui/App.vue` 目前已经拆成薄壳，新增能力优先落在 `ui/components/*`、`ui/composables/*`、`ui/constants/*`、`ui/styles/*`
- `弥散渐变` 继续保持模块化，后续调整优先改 `DiffuseGradientTab` 与 `useMeshGradient`，不要回填到 `App.vue`

## 备注

- 本文件是当前任务的短期状态源
- 更长的背景说明保留在 `AGENT.md`
- 后续所有回复默认使用中文
- 回复中只提供简要推理说明，不展开内部思考过程
- 对外可见的分析、步骤说明和结论都使用中文
